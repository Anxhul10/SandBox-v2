name: next build
on: 
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: repo
        uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - name: install
        run: pnpm install
      - name: build
        run: pnpm build
  